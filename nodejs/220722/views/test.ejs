<html>
    <head>
        <title>EJS</title>
    </head>
    <body>
        <p>
            <%=a%>
        </p>
        <% for ( var i = 0; i < list.length; i++) { %> 
            <h1><%=list[i]%></h1>     
            <!-- //index.js에서 list:list를 배열로 했기 때문에 받을 수 있는것 -->
            <!-- <% if (i == 3 ) { %>
                <h3>hi</h3>
            <% } else { %>
                <h1>hi</h1>
            <% } %> -->
        <% } %>

        <% var a = 'hello'; %>
        <p> <%=a%> </p>
        <%- include ('test2.ejs') %>

    </body>
</html>
